name = "broken_fastapi_service"
description = "Fix a broken FastAPI service that fails to start due to dependency and startup issues."

[environment.runtime]
python = "3.12"

[solution]
entrypoint = "bash solution/solve.sh"

[test]
entrypoint = "bash tests/test.sh"
